'use server';

/**
 * @fileOverview Placeholder for deprecated OneDrive export action.
 * Synchronization logic has been decoupled from the primary billing workflow.
 */

export async function exportInvoiceToOneDrive() {
  return { 
    success: false,
    message: "Module deprecated."
  };
}
