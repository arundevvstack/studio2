rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * MEDIAFLOW SECURITY MODEL (PROTOTYPE VERSION)
     * 
     * Full read/write access allowed for rapid prototyping.
     */
    match /{document=**} {
      allow read, write: if true;
    }
  }
}