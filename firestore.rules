
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TESTING MODE: Allows all reads and writes. 
    // This ensures no "Missing or insufficient permissions" errors during prototyping.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
