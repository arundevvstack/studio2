rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // TEMPORARY DEBUG MODE - UNIVERSAL ACCESS
    // FORCE SYNC TRIGGER: 2026-02-24T09:00:00Z
    match /{document=**} {
      allow read, write: if true;
    }

  }
}