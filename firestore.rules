rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    /**
     * MEDIAFLOW OPEN ACCESS CORE
     * All authentication and role-based restrictions have been removed.
     */
    match /{document=**} {
      allow read, write: if true;
    }
  }
}