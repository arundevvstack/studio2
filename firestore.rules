
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Testing mode: allow all authenticated users full access to documents
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
