
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    /**
     * MEDIAFLOW STRATEGIC SECURITY CORE
     */

    function isSignedIn() {
      return request.auth != null;
    }

    // Helper to get the user's registry document
    function getUserData() {
      let userPath = /databases/$(database)/documents/users/$(request.auth.uid);
      return exists(userPath) ? get(userPath).data : null;
    }

    // Authoritative check for Strategic Permit & Active Status
    function hasStrategicPermit() {
      let user = getUserData();
      return isSignedIn() && (
        (request.auth.token.strategicPermit == true && request.auth.token.status == "active") ||
        (user != null && user.strategicPermit == true && user.status == "active")
      );
    }

    // Check for Root Administrative Authority
    function isAdmin() {
      let user = getUserData();
      return isSignedIn() && (
        request.auth.token.role == "admin" ||
        (user != null && user.role == "admin")
      );
    }

    /**
     * IDENTITY & AUTHORIZATION REGISTRY
     */
    match /users/{userId} {
      // Users can read their own profile; Admins can see everyone
      allow get: if isSignedIn() && (request.auth.uid == userId || isAdmin());
      // Admins need to list users for the Identity Ledger
      allow list: if isSignedIn() && isAdmin();
      // Allow initial provisioning during sign-up
      allow create: if isSignedIn() && request.auth.uid == userId;
      // Users can update their own metadata (UI gates sensitive fields); Admins can update any
      allow update: if isSignedIn() && (request.auth.uid == userId || isAdmin());
      // Admins can purge identities from the registry
      allow delete: if isSignedIn() && isAdmin();
    }

    /**
     * ORGANIZATIONAL INFRASTRUCTURE
     */
    match /teamMembers/{memberId} {
      // Users need to read their own record even if pending; Experts see the whole team
      allow read: if isSignedIn() && (request.auth.uid == memberId || hasStrategicPermit() || isAdmin());
      // Allow self-updates for professional profile management
      allow update: if isSignedIn() && (request.auth.uid == memberId || isAdmin());
      // Only admins can create new records via the team form
      allow create: if isSignedIn() && isAdmin();
      allow delete: if isSignedIn() && isAdmin();
      
      // Allow session tracking for the authenticated user
      match /sessions/{sessionId} {
        allow read, write: if isSignedIn() && request.auth.uid == memberId;
      }
    }

    match /roles/{roleId} {
      // All users need to read roles to resolve nomenclature
      allow read: if isSignedIn();
      allow write: if isSignedIn() && isAdmin();
    }

    /**
     * CORE PRODUCTION ENTITIES
     */
    match /projects/{projectId} {
      allow read, write: if hasStrategicPermit() || isAdmin();
      
      // Nested task roadmap
      match /tasks/{taskId} {
        allow read, write: if hasStrategicPermit() || isAdmin();
      }
    }

    match /clients/{clientId} {
      allow read, write: if hasStrategicPermit() || isAdmin();
      
      // Client-specific billing
      match /invoices/{invoiceId} {
        allow read, write: if hasStrategicPermit() || isAdmin();
      }
    }

    match /leads/{leadId} {
      allow read, write: if hasStrategicPermit() || isAdmin();
    }

    match /talents/{talentId} {
      allow read, write: if hasStrategicPermit() || isAdmin();
    }

    match /shoot_network/{talentId} {
      allow read, write: if hasStrategicPermit() || isAdmin();
    }

    match /proposals/{proposalId} {
      allow read, write: if hasStrategicPermit() || isAdmin();
      allow delete: if isAdmin();
    }

    /**
     * APP CONFIGURATION & SETTINGS
     */
    match /settings/{settingId} {
      // Basic settings like project verticals need to be readable for provisioning
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    match /companyBillingSettings/{id} {
      allow read: if isSignedIn() && (hasStrategicPermit() || isAdmin());
      allow write: if isAdmin();
    }

    match /workflowSettings/{id} {
      allow read: if isSignedIn() && (hasStrategicPermit() || isAdmin());
      allow write: if isAdmin();
    }

    /**
     * PIPELINE & MARKETING INTELLIGENCE
     */
    match /followUps/{id} {
      allow read, write: if hasStrategicPermit() || isAdmin();
    }

    match /campaigns/{id} {
      allow read, write: if hasStrategicPermit() || isAdmin();
    }
  }
}
