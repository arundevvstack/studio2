
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // --- GLOBAL HELPERS ---
    function isSignedIn() {
      return request.auth != null;
    }
    
    // Simplified admin check for prototype: currently checks for existence in roles_admin
    function isAdmin() {
      return isSignedIn() && exists(/databases/$(database)/documents/roles_admin/$(request.auth.uid));
    }

    // --- COLLECTION RULES ---

    // Administrative roles
    match /roles_admin/{userId} {
      allow get: if isSignedIn() && request.auth.uid == userId;
      allow list, create, update, delete: if false; // Only manageable via console for security
    }

    // Shoot Network Repository
    // Relaxed to allow write for all signed-in users for the prototype
    match /shoot_network/{talentId} {
      allow read, write: if isSignedIn();
    }

    // Projects
    match /projects/{projectId} {
      allow read, write: if isSignedIn();
      
      // Sub-collections for projects
      match /tasks/{taskId} { allow read, write: if isSignedIn(); }
      match /tickets/{ticketId} { allow read, write: if isSignedIn(); }
    }

    // Clients
    match /clients/{clientId} {
      allow read, write: if isSignedIn();
      match /invoices/{invoiceId} { allow read, write: if isSignedIn(); }
    }

    // Pipeline Leads
    match /leads/{leadId} {
      allow read, write: if isSignedIn();
    }

    // Team Members
    match /teamMembers/{memberId} {
      allow read, write: if isSignedIn();
    }

    // Fallback for other potential collections (Intelligence Hub, etc.)
    match /{document=**} {
      allow read, write: if isSignedIn();
    }
  }
}
